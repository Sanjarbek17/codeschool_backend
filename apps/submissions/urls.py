from django.urls import path
from . import views

app_name = 'submissions'

urlpatterns = [
    # URL patterns will be added here when views are created
    # Example:
    # path('submit/', views.SubmissionCreateView.as_view(), name='submit'),
    # path('submissions/', views.SubmissionListView.as_view(), name='submission-list'),
    # path('evaluate/', views.EvaluateSubmissionView.as_view(), name='evaluate'),
]
